# Маркетинг: прогноз вероятности покупки


## 💡 Описания проекта
Проект анализирует поведение пользователей интернет-магазина с целью предсказания, кто с высокой вероятностью совершит покупку в ближайшие 90 дней. На основе истории покупок и рассылок создаются признаки, которые затем используются в моделях машинного обучения (LightGBM, XGBoost, SVM). Основной показатель качества — ROC-AUC.  

## Цель
Предсказать вероятность того, что пользователь совершит покупку в течение **90 дней** после получения рекламного сообщения.  
Это может помочь оптимизировать маркетинговые рассылки и планирование продаж.

---

## 
Используются три основных источника:

### `apparel-purchases` – история покупок
- `client_id` — идентификатор пользователя  
- `quantity` — количество товаров в заказе  
- `price` — цена товара  
- `category_ids` — вложенные категории товара  
- `date` — дата покупки  
- `message_id` — идентификатор сообщения из рассылки  

### `apparel-messages` – история рекламных рассылок
- `bulk_campaign_id` — идентификатор кампании  
- `client_id` — идентификатор пользователя  
- `message_id` — идентификатор сообщения  
- `event` — тип действия  
- `channel` — канал рассылки  
- `date` — дата рассылки  
- `created_at` — точное время создания сообщения  

### `apparel-target_binary` – целевой признак
- `client_id` — идентификатор пользователя  
- `target` — совершит ли клиент покупку в течение 90 дней  

### Особенности данных
- Пересечение уникальных клиентов в `apparel-messages` с клиентами в `apparel-target_binary` и `apparel-purchases` составляет **85%**, что указывает на возможные ошибки в данных. 
- Сообщения не имеют двух получателей в `apparel-messages`, но появляются в `apparel-purchases`.  
- Некоторые сообщения из `apparel-messages` характеризуются странным поведением клиентов
- Аномалии выявляются в процессе анализа данных.

---

##  Методы машинного обучения
- **LightGBM** — градиентный бустинг  
- **XGBoost** — градиентный бустинг  
- **SVM** — метод опорных векторов  

##  Метрика
- **ROC-AUC**  

---
##  Результат 
- **ROC-AUC = 0.78** 
